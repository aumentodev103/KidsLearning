import 'package:flutter/material.dart';

class ContentListTile extends StatelessWidget {
  const ContentListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Title");
  }
}
